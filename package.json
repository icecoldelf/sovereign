{
  "name": "sovereign",
  "version": "1.0.0",
  "description": "wget https://apt.puppetlabs.com/puppet5-release-xenial.deb sudo dpkg -i puppet5-release-xenial.deb sudo apt-get update sudo apt-get install puppet",
  "main": "app.js",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react": "^6.24.1"
  },
  "start": "node server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel src -presets react -out-dir static",
    "watch": "babel src -presets react -out-dir static -watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/icecoldelf/sovereign.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/icecoldelf/sovereign/issues"
  },
  "homepage": "https://github.com/icecoldelf/sovereign#readme"
}
